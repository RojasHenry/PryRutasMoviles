<?xml version="1.0" encoding="UTF-8"?>
<popups:PopupPage
    xmlns:popups="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="PryRutasMoviles.Pages.SelectTypeModal">
    <StackLayout HorizontalOptions="Center"
                 VerticalOptions="Center">
        <Grid BackgroundColor="WhiteSmoke" 
          HorizontalOptions="Center"
          VerticalOptions="Center"
          RowSpacing="10" 
          ColumnSpacing="10" 
          Padding="10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />                
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnPassenger" Grid.Row="0" Grid.Column="0" Text="Pasajero" Clicked="btnPassenger_Clicked"/>
            <Button x:Name="btnDriver" Grid.Row="0" Grid.Column="1" Text="Conductor" Clicked="btnDriver_Clicked"/>
        </Grid>
    </StackLayout>    
</popups:PopupPage>
